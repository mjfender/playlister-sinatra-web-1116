<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <form action="/songs" method="POST">
    <label for="Name">Name:</label><input type="text" name="song[name]" id="Name"><br>
    <label for="Artist Name">Artist Name:</label>
    <input type="text" name="artist[name]" id="Artist Name"><br>

    <strong>Genres:</strong><br>
    <% @genres.each do |genre| %>

    <label for="<%= genre.name %>"> <%= genre.name %> </label>
    <input type="checkbox" name="song[genre_ids][]" value="<%= genre.id %>" id="<%= genre.name %>"><br>
    <% end %>
    <label for="New Genre">New Genre</label>
    <input type="text" name="genre[name]" id="Genre Name"><br>

    <input type="submit" value="Create">

  </form>

</body>
</html>
